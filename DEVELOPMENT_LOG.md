# AI 开发记录（DEVELOPMENT LOG）

> 记录本项目（网页心理测评系统）AI 辅助开发过程中的 bug、排查过程与修复方案，以及测试验证结论。
> 开发日期：2026-09-12

## 一、开发顺序回顾

按既定顺序完成：

1. 项目框架：`package.json` / `server.js`（Express）+ 4 个页面骨架；
2. 问卷表单与数据存储：题库渲染、单选交互、必填校验、`POST /api/responses` JSON 落盘（临时文件 + 原子重命名）；
3. 计分逻辑：`lib/questions.js` 统一实现反向题转换（6 − x）与维度均分；
4. 结果可视化：Chart.js 雷达图 + 得分条 + 分档中性解读；
5. 管理后台：均分/分布/单题分布/Cronbach's α/相关性统计。

## 二、Bug 与问题记录

### Bug 1：Cronbach's α 未先做反向题转换，B5 各维度 α 计算出极大的负值（严重，已修复）

- **现象**：端到端统计核对时，服务端返回的大五各维度 α 为 `-17.6`、`-92.4` 等异常值（期望约 0.9x），而 AI 态度量表（全正向题）的 α 正常（0.976）。
- **排查**：用独立实现的统计脚本（不依赖 `lib/stats.js`）比对均分、分布、相关、α。均分、相关、p 值、AI 态度 α 全部一致，仅「含反向题的量表」α 不一致 → 定位到 α 计算的输入数据。
- **根因**：`server.js` 的 `buildAdminStats` 计算 α 时直接取原始作答 `r.answers[i]`，没有对反向题先做 `6 − 原始分` 的转换。反向题与正向题未同向化，题目间相关为负，代入 α 公式后得到严重失真的负值。维度均分的计算路径正确（在 `computeScores` 内已转换），因此均分/相关未受影响，问题只在 α。
- **修复**：增加 `itemScoreOf(r, i) = QUESTIONS[i].reverse ? 6 - r.answers[i] : r.answers[i]`，α（各维度与大五总量表）均基于转换后的题目分计算；并在代码处补充注释说明该陷阱。
- **验证**：重启服务后重新核对，全部 α 与独立实现一致（如大五总量表 α = 0.816）。
- **教训**：信度分析必须先做题目同向化（reverse-code）再进公式；「部分指标正确、部分指标错误」时，优先比对两组指标的数据来源差异。

### Bug 2：管理后台 α 列表重复渲染「大五总量表」行（自审发现，提交前已修复）

- **现象**：`admin.js` 的 `renderAlpha` 既在 map 中给 `bigfive_total` 生成了一行（还会拼出 `大五总量表（10题）（）` 的空括号文案），map 之后又额外 push 了一行，造成重复。
- **修复**：统一改为 `[...DIM_ORDER, 'bigfive_total']` 一次遍历生成，题目数文案集中映射（ai_attitude→6题、bigfive_total→10题、其余→2题）。
- **教训**：同一数据源的两处拼接逻辑应合并；渲染函数中「先 filter 再补充」的写法容易产生重复行。

### 问题 3：统计核对脚本的「假报警」——期望值与实际数据条数不一致（测试工具问题，已修正）

- **现象**：首次独立核对时所有均分都对不上（如外倾性 got 3.36 / expect 3.2）。
- **排查**：检查发现核对脚本按 10 份模拟数据计算期望值，但服务器数据文件里还残留此前冒烟测试提交的 1 份记录（共 11 份）。用均分反推验证：3.2×10 + 5 = 37，37/11 = 3.36，与服务端完全吻合，说明**服务端计算正确，是核对工具的口径错了**。
- **修复**：核对脚本改为直接读取 `data/responses.json` 的全部真实记录计算期望值，并在导入前重置数据文件，消除口径差。
- **教训**：做「独立比对」时，期望值必须与被测对象基于**同一份实际数据**计算，而不是基于测试夹具；对不上时先用数值反推判断是哪一侧的问题。

### 问题 4：p 值自检的「期望值」本身算错（测试预期错误，已修正）

- **现象**：核对脚本注释写 `r=0.7071, n=6 → p≈0.182`，程序输出 0.1161，触发怀疑。
- **排查**：手算复核：r = 1/√2 ≈ 0.7071，n = 6 → t = r·√((n−2)/(1−r²)) = 2.0，df = 4；双尾 p = I₍0.5₎(2, 0.5) = 0.1161。程序输出正确，注释里的 0.182 是手算错误。
- **修复**：修正核对注释为解析值 0.1161，程序输出与解析值小数点后 4 位一致。
- **教训**：统计实现验证时，「期望值」本身也要经过解析推导或权威表核对，避免用第二个手算错误掩盖实现正确性。

### 问题 5：浏览器端到端测试两次因步数预算中断（流程问题，调整策略后通过）

- **现象**：browser agent 逐题点击 16 道单选题消耗大量步骤，两次都在答题阶段耗尽预算，未走到结果页与管理后台验证。
- **调整**：改为「API 预置数据 + 浏览器只验证渲染」：先用 `POST /api/responses` 快速写入 2 份已知作答，再让浏览器直接访问 `/result?id=...` 与 `/admin` 做断言与截图。
- **结果**：结果页（雷达图 canvas、维度得分条、分档标注、AI 得分、免责声明）与管理后台（口令错误提示、总人数、α 列表、均分图表、分布图、单题分布表、相关性表）全部渲染正确。
- **教训**：UI 自动化测试把「造数据」与「验渲染」解耦，用 API 造数可大幅降低浏览器步骤消耗。

### 问题 6：Windows PowerShell 环境差异（小问题，随做随改）

- 管道 `head -c 400` 在 PowerShell 不存在（`head` 非内建命令），改用 PowerShell 原生方式处理 JSON（`Invoke-RestMethod` 直接返回对象），不再手动截断输出。
- 布尔型结论：跨平台脚本避免使用 Unix 专属命令，或统一交给 Node.js 脚本完成（本项目后续验证均改用 Node 脚本）。

### Bug 7：PowerShell 写入 UTF-8 BOM 导致 JSON 解析失败，且失败回退有数据覆盖风险（严重，已修复）

- **现象**：交付前的服务器日志检查发现 `[data] responses.json 读取失败：Unexpected token '﻿'`。此前用 `Set-Content -Encoding UTF8` 重置数据文件时，Windows PowerShell 5.1 会写入 **UTF-8 BOM（U+FEFF）**，`JSON.parse` 遇 BOM 直接抛错。
- **风险分析**：比解析失败本身更严重的是当时的回退逻辑——`readResponses` 解析失败时静默返回 `[]`，下一次 `POST /api/responses` 会把"仅含新纪录的数组"整体写回文件，**历史数据被静默清空**。本次恰逢文件内容只有 `[]` 故未造成实际损失，但属于真实的数据丢失隐患。
- **修复**（`server.js` 的 `readResponses`）：
  1. 读取后先剥离行首 BOM（`charCodeAt(0) === 0xFEFF` 则 `slice(1)`），兼容用 PowerShell/记事本手工编辑过的数据文件；
  2. 解析失败时不再静默当作空库：先把原文件重命名为 `responses.json.corrupt-<时间戳>.bak` 备份供人工恢复，再返回 `[]`，并在日志中醒目提示。
- **验证**：重启后回归测试通过——带 BOM 的数据文件下 POST 成功且统计读到 1 人（BOM 已容忍）；写入损坏内容后请求统计，自动生成 `.corrupt-*.bak` 备份文件；交付前用 Node 重写为无 BOM 的 `[]`（首字节 91=`[`）。
- **教训**：① Windows PowerShell 的 `-Encoding UTF8` ≠ 无 BOM UTF-8，跨工具写文件后要检查首字节；② 「解析失败回退为空集合」的模式在"读改写整文件"的存储方案里等于把损坏升级为数据丢失，损坏时必须先隔离备份。

## 三、测试验证结论（交付前最终状态）

- **页面路由**：`/`、`/survey`、`/result`、`/admin` 均返回 200。
- **提交校验**：缺题（15 题）、超界值（6）、非整数（2.5）均返回 400；合法提交返回 id 与计分。
- **计分正确性**：已知作答 `[5,1,5,1,5,1,1,5,5,1,5,...]` 验证反向计分（E2=1→5，N2=5→1），维度分输出符合手工推导。
- **统计正确性**：10 份模拟数据经独立实现比对，均分、0.5 分档分布（含 5.0 落入末档）、单题分布、各 α、Pearson r、双尾 p 全部一致；p 值与解析解 0.1161 一致。
- **鉴权**：无口令与错误口令访问 `/api/admin/stats` 均返回 401。
- **浏览器 E2E**：知情同意门控（未勾选按钮禁用）→ 问卷（进度条、必填校验红框与提示）→ 提交 → 结果页渲染 → 管理后台渲染，全部通过；console 无业务 JS 错误（仅 Tailwind CDN 的非生产环境提示）。
- **数据健壮性回归**：UTF-8 BOM 数据文件可正常读写；损坏文件自动备份为 `.corrupt-*.bak`；交付文件为无 BOM 的 `[]`（空库，首字节已核验）。
- 交付前已将 `data/responses.json` 重置为 `[]`，并删除临时验证脚本。

## 四、遗留说明与改进建议

- 管理后台口令为服务端 API 级校验（`x-admin-key` 请求头），页面本身为客户端门控；正式部署请修改 `ADMIN_PASSWORD`，必要时可加 HTTPS 与会话机制。
- TailwindCSS 与 Chart.js 走 CDN，离线环境需本地化（见 README 部署章节）。
- 2 题维度的 Cronbach's α 解释力有限（等价于 2r/(1+r)），正式研究建议扩充每维度题量。
- 并发写入以「临时文件 + rename」原子替换兜底；如后续部署多实例，需要改为文件锁或迁移到数据库。

---

## 五、迭代记录（2026-09-12）：管理员后台新增「用户原始记录列表」

### 需求与实现

1. **完整记录保存**：核对 `POST /api/responses` 既有落盘结构，已包含 UUID、ISO 提交时间、16 题原始分值数组、6 个维度得分（大五 5 + AI 态度），需求第 1 点在首版即满足，本次未改动存储结构。
2. **新增接口** `GET /api/admin/records`：与 `/api/admin/stats` 相同的 `x-admin-key` 口令校验（经验：新增管理接口必须复用同一鉴权，避免出现「登录只是 UI 门槛」）；返回记录（按提交时间倒序）+ 题目元信息（题号/题面/维度/反向标记）+ 李克特选项文案，供详情区一次性渲染，无需逐行再请求。
3. **前端**：admin.html 新增板块；admin.js 与统计接口 `Promise.all` 并行拉取；事件委托实现「查看详情 / 收起详情」展开行（`colspan=9` 的次级 `<tr>`，16 张题目卡片双列排布，展示原始分值 + 选项中文文案 + 反向标签）；空数据有占位行。原有统计渲染逻辑零改动。

### 本轮遇到的问题

- **问题 A（环境，非代码缺陷）：浏览器缓存旧版 admin 页面导致新板块「消失」**
  - 现象：接口直连（curl / Invoke-RestMethod）返回正确，服务端日志无异常；但浏览器里统计板块正常、新记录列表板块整个不存在。
  - 排查：静态板块标题写死在 HTML 中、与 JS 无关却缺失 → 判定为浏览器命中旧缓存；curl 实际下发的 HTML/JS 均含新代码，证实服务端无误。
  - 解决：用全新标签页访问带版本号查询参数的 URL（`/admin?v=20260912b`）绕缓存后全部正常。
  - 教训：前端改版后的 UI 自动化验证要带 cache-buster 或无缓存上下文；「接口对、页面缺静态区块」是典型缓存症状，不必怀疑渲染代码。
- **问题 B（瞬时）：首次浏览器验证时 `/api/admin/stats` 报 `net::ERR_ABORTED`**：发生在服务重启与页面加载的时间重叠窗口，服务健康检查与重试后恢复，非缺陷。

### 验证结论

- 鉴权：无口令 / 错误口令访问新接口均 401。
- 数据：4 条造数记录字段完整（16 原始分 + 6 维度分），倒序正确；抽核外倾性=5.00、神经质=1.00、AI=4.33 与计分规则一致；JSON 文件内无重复写入。
- 兼容：原统计接口与板块（人数、α、均分、分布、单题分布、相关性）不受影响。
- 浏览器 E2E：列表 9 列、4 行、详情展开/收起、16 题卡片与反向标签均通过，console 无业务错误。
- 交付前已将 `data/responses.json` 用 Node 重置为无 BOM 的 `[]`。

---

## 六、v2 大版本迭代（2026-09-12）：量表升级 16 题 → 40 题，AI 量表拆 3 子维度

### 变更范围

1. **题库重构（lib/questions.js）**：大五 25 题（每维 5 题，题 ID 用 O/C/E/A/N1–5）、AI 15 题拆为收益感知/风险感知/使用意愿（BEN/RISK/USE1–5）；维度键 8 个挂题维度 + 派生的 `ai_attitude` 总态度；计分函数改为按维度通用聚合（不依赖固定题数），AI 总分单独按 15 题总平均计算；记录新增 `version: 2`。
2. **结果页**：雷达图保持大五 5 维；AI 板块改为「总态度 + 3 子维度」，解读卡片 5→9 张；**风险感知按独立构念表述**（高分=风险意识强，不等于否定 AI），所有文案继续保持非诊断口径。
3. **管理后台**：均分/分布覆盖 9 个分数维度；α 改为 8 个 5 题维度 + 大五总量表（25）+ AI 总量表（15）；相关性由「5 行单列」升级为 **5×4 矩阵**（3 子维度 + 总态度，每格 r 与双尾 p）；记录表扩为 12 列，详情 40 题卡片。
4. **旧数据防护**：`filterCurrentRecords()` 按题数过滤，旧版 16 题记录不进入统计/列表但保留在 JSON 文件并告警，避免维度分得 NaN。

### 本轮遇到的问题

- **问题 A（需求核对要点）：反向题位置不能按固定规律假设**
  - 初看以为每组 5 题的反向题都在第 2、4 位，实际**开放性组的反向题在第 3、5 位**（第 3 题"很少主动尝试全新事物"、第 5 题"不喜欢打破常规"），其余七组才是第 2、4 位。
  - 处理：逐题按用户给定的正/反标记显式编码，并在核对脚本中用反向题号数组 `[3,5,7,9,12,14,…,37,39]` 断言，防止手滑；题库注释也写明"位置不可按规律推测"。
- **问题 B（测试脚本，重犯一次的老问题）：期望值未以实际落盘数据为基准**
  - 现象：脚本先提交 2 条极值用例（全 5/全 1）再提交 12 条模拟数据，却只按 12 条模拟数据算期望，导致 n=14、均分/α/相关全部"对不上"。与第四节记录的 10 vs 11 条事故同源。
  - 修复：改为从 `/api/admin/records` 拉回**服务端实际记录**再独立重算（n=14），随即全部通过。
  - 教训（应固化）：凡做独立比对，期望值与被测对象必须基于同一份实际数据；测试夹具与落盘记录混用时，以接口返回为准。
- **问题 C（测试脚本）：AI 总分是派生维度、没有挂载题目**
  - 独立脚本用 `dimIdx['ai_attitude']` 取题导致 `undefined.map` 崩溃；`ai_attitude` 由 3 个子维度的 15 题合成。修复：`valsOf()` 对总分特判为 3 组题索引的并集。也反向验证了产品代码里"总分单独计算、不参与题目分组"的结构是必要的。
- **构念设计提醒：AI 风险感知高分的解读方向**。风险题按题面反向规则同向化后，高分=感知风险多。若把它和"态度积极程度"混为一谈会误读，因此结果页/后台/README 三处都显式说明它是风险意识指标，总态度需结合子维度理解。

### 验证结论（14 份样本，独立实现比对）

- 提交校验：39 题、41 题均 400；极值作答（正5反1→全 5；正1反5→全 1）正确，抽查风险维度 [3,4,2,2,3]→2.8、AI 总分 4.27 与手工推导一致。
- 统计：9 个维度均分与 0.5 分档分布、40 题单题分布、10 个 α（8 维度+2 总量表，示例：大五总量表 0.966、AI 总量表 0.881）、5×4 相关矩阵的 20 个 r 及 p 值，全部与独立实现一致。
- 兼容：混入 1 条旧版 16 题记录后，统计/列表均正确跳过（n 不变、无 null/NaN），旧记录仍保留在文件中。
- 浏览器 E2E：知情同意新文案 → 40 题问卷（分组、进度 40/40、必填红框）→ 结果页（雷达图、总态度 4.27、3 子维度、9 张解读卡片）→ 后台（人数、α 列表、9 维分布图、40 行单题表、5×4 矩阵、12 列记录表、40 题详情展开）全部通过，console 无业务错误。
- 交付前已删除临时核对脚本，并将 `data/responses.json` 重置为无 BOM 的 `[]`。
